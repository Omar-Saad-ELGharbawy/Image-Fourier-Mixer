import React from "react";
import UploadImage from "../components/UploadImage/index";

const Layout = () => {
  return (
    <div>
      <UploadImage type={1} />
    </div>
  );
};

export default Layout;
